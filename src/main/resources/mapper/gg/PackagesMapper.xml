<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lmserver.mapper.gg.PackagesMapper">

    <select id="selectPackagesByProductId" resultType="com.lmserver.dto.response.PackageDto">
        SELECT id, product_id, series_name, package_name, url, status, created_at
        FROM packages
        WHERE product_id = #{productId}
        ORDER BY created_at DESC
    </select>

    <select id="selectPackagesByProductIds" resultType="com.lmserver.dto.response.PackageDto">
        SELECT id, product_id, series_name, package_name, url, status, created_at
        FROM packages
        WHERE product_id IN
        <foreach collection="productIds" item="pid" open="(" separator="," close=")">
            #{pid}
        </foreach>
        ORDER BY created_at DESC
    </select>

</mapper>
